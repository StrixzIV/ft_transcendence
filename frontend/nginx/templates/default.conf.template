server {
    # Port
    listen 8080;

    # Main properties
    server_name ${DOMAIN_NAME};
    root /var/www/html;
    index index.html;

    # For fetching frontend page
    location / {
        try_files $uri $uri/ =404;
    }
}