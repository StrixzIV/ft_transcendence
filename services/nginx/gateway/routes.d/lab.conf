# Route to RESTFUL endpoint
location /lab/api/ {
    proxy_pass http://lab:3000;
    include /etc/nginx/rules.d/restful-proxy-rule.conf;
}

# Route to WS endpoint
location /lab/ws/ {
    proxy_pass http://lab:3000;
    include /etc/nginx/rules.d/ws-proxy-rule.conf;
}