{

    "name": "ft-transendence-auth",
    "main": "dist/server.js",

    "scripts": {
        "dev": "ts-node-dev --respawn srcs/server.ts",
        "build": "tsc",
        "start": "node dist/server.js",
        "prisma": "prisma",
        "db_init": "npx prisma migrate dev --name init",
        "generate": "npx prisma generate"
    },

    "dependencies": {
        "@fastify/cors": "^11.0.1",
        "@prisma/client": "^6.9.0",
        "bcrypt": "^6.0.0",
        "fastify": "^5.4.0",
        "node-vault": "^0.10.5",
        "request": "^2.88.2"
    },

    "devDependencies": {
        "@types/bcrypt": "^5.0.2",
        "@types/request": "^2.48.12",
        "prisma": "^6.9.0",
        "ts-node-dev": "^2.0.0",
        "typescript": "^5.0.0"
    }

}

